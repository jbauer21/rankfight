# Environment Configuration Example
# Copy this file to .env and update with your actual values

# Secret key for Flask sessions (IMPORTANT: Generate a strong random key for production!)
# You can generate one using: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your-secret-key-here

# Optional: Port configuration (default: 8000)
# PORT=8000

# Optional: Host configuration (default: 0.0.0.0)
# HOST=0.0.0.0
